return {
    ScanDelay = 1, --in seconds

    AllowedTypes = {
        Reactor = "BigReactors-Reactor",
        Turbine = "BigReactors-Turbine"
        },

    AllowedMax = {
        Monitors = 1
        Reactor = 5,
        Turbine = 20
    }
}
